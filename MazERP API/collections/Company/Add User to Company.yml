info:
  name: Add User to Company
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5148/api/company/assign
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
